json.extract! @user,
              :nickname,
              :id,
              :fname,
              :lname,
              :email,
              :points
# json.profile_pic_url asset_path(@user.profile_pic.url(:original))
# end
